using UnityEngine;

/// <summary>
/// 노드(맵)가 클리어 되었을 때, 다음 노드(맵)으로 이동 시켜주는 역할
/// </summary>
public class NextNodeTeleporter : MonoBehaviour
{
    // Node.NodeState.Clear일때 다음 Node로 이동 하는 영역을 표시
    // 모든 플레이어가 규칙에 따라(과반) 모일 때 다음 영역으로 이동
}
