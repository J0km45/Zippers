using UnityEngine;

/// <summary>
/// GameScene 진입 시 Map을 생성해주는 역할
/// </summary>
public class MapMaker : MonoBehaviour
{
    // NodeTreeMaker의 작동이 끝나면 각 노드에 해당 되는 맵을 지정 위치로 이동 하고 Enable하는 역할
    // 앞 노드가 배치 되면 다음 노드의 Node.StartDir를 읽어 다음 배치 위치 지정
}
